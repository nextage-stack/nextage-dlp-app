# Webpack build-time configuration
# Copy to .env (git-ignored) for local overrides

# Azure Functions URL — used by the add-in to call the backend proxy
# DEV: https://localhost:7071/api
# PROD: https://nextage-dlp-api.azurewebsites.net/api
AZURE_FUNCTIONS_URL=https://localhost:7071/api
